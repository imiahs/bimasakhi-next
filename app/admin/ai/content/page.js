import ContentRecContent from '@/features/admin/ai/ContentRecContent';

export const metadata = {
    title: 'AI Content Engine | Bima Sakhi Admin',
    description: 'AI-assisted content recommendation and drop-off analysis.',
};

export default function AIContentPage() {
    return <ContentRecContent />;
}
