import RecruiterContent from '@/features/admin/ai/RecruiterContent';

export const metadata = {
    title: 'AI Recruiter | Bima Sakhi Admin',
    description: 'AI-assisted lead recruitment and conversion engine.',
};

export default function AIRecruiterPage() {
    return <RecruiterContent />;
}
