export const metadata = {
    robots: {
        index: false,
        follow: false,
    },
    title: 'Admin Panel | Bima Sakhi',
};

export default function AdminLayout({ children }) {
    return children;
}
