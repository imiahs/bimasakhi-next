import LoginContent from '@/features/admin/login/LoginContent';

export const metadata = {
    title: 'Admin Login | Bima Sakhi',
    robots: { index: false, follow: false },
};

export default function LoginPage() {
    return <LoginContent />;
}
