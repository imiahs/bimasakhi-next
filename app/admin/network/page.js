export const metadata = {
    title: 'DO Network Command Center',
    description: 'Manage network metrics, activations, and total business.',
};

import AdminNetworkContent from '@/features/admin/network/AdminNetworkContent';

export default function AdminNetworkPage() {
    return <AdminNetworkContent />;
}
