import React from 'react';
import IndexHealthContent from '@/features/admin/seo/IndexHealthContent';

export const metadata = {
    title: 'Index Health | Bima Sakhi OS',
};

export default function IndexHealthPage() {
    return <IndexHealthContent />;
}
